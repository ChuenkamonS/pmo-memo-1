// ─────────────────────────────────────────────
//  views/budget.js  —  Budget Monitor
// ─────────────────────────────────────────────
// Data source: loadMemos() filtered by status === 'completed'
// Memo object: { memoNo, type, project, total, status, createdAt, updatedAt }

function renderBudgetMonitor() {
  // TODO: implement
  // Suggested structure:
  // 1. 4 metric cards: total this month, completed count, top type, top project
  // 2. Table by type: SL/HW/INT/ENT/DEP with count + total
  // 3. Table by project with total + progress bar vs ceiling
  // 4. Date range filter (this month / last month / 3 months / all)
}
